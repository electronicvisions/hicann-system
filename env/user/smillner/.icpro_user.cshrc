# $ICPRO_DIR/env/user/smillner/.icpro_user.cshrc
# (schlue) 2007/09/08
#
# This file will be sourced from every subshell (tcsh) spawned
# within a ICPROJECT.
# - user specific project alias definitions
# - NOT for loading of modules or environment settings
#   (put them into ./env/user/smillner/.icpro_user.login)
#
# Project:    p_facets
# Subproject: s_system
#
#####################################################
# Example:
#alias lsv 'ls *.v*'

alias  i        'icutil whereami'
